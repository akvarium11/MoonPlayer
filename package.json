{
  "name": "moonplayer",
  "version": "2.4.0",
  "description": "Local browser-based music player with dynamic island and smart search",
  "main": "main.js",
  "scripts": {
    "start": "tauri dev",
    "dev": "tauri dev",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build && node scripts/package-portable.js",
    "build": "tauri build && node scripts/package-portable.js",
    "server": "node server.js",
    "package": "node scripts/package-portable.js",
    "build:android": "cd android && .\\gradlew.bat assembleDebug",
    "start:electron": "electron .",
    "build:electron": "electron-builder --win portable",
    "dist": "tauri build && node scripts/package-portable.js"
  },
  "keywords": [],
  "author": "akvarium11",
  "license": "ISC",
  "dependencies": {
    "discord-rpc": "^4.0.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "electron": "^44.1.1",
    "electron-builder": "^26.15.3"
  },
  "build": {
    "appId": "com.moonplayer.desktop",
    "productName": "MoonPlayer",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.ico"
    },
    "portable": {
      "artifactName": "MoonPlayer.exe"
    },
    "asar": true,
    "files": [
      "main.js",
      "server.js",
      "soundcloud_service.js",
      "discord_presence.js",
      "package.json",
      "assets/**/*",
      "public/**/*",
      "node_modules/**/*"
    ]
  }
}
